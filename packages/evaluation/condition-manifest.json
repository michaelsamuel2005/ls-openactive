{
  "_comment": "AI-ASSISTED SCAFFOLD from CLARENCE_ZHEN_JIN_TAN_SPECIALISED_WORK_PACKAGE.md §13 (C-17) resolving C-BLOCK-06. NOT authorship evidence. Fahmi owns estimands/analysis; Clarence supplies the frozen builds/manifests. Every condition in a family shares the SAME terminal DecisionEnvelope per task — only the interface differs — so an interface effect is not confounded with a backend difference; L_reliance binds to that shared decision (CA-1 / C-BLOCK-15).",
  "version": "0.1.0-PROPOSED",
  "status": "PROPOSED",
  "principle": "Within a surface family, all conditions resolve the identical terminal DecisionEnvelope per task; only presentation/interaction differs (C-BLOCK-06). validate_conditions.py checks projected-digest equality across the family.",

  "public_conditions": [
    { "id": "P0", "surface": "public",
      "label": "incumbent-informed structured finder / list control (same project backend)",
      "capabilities": { "evidence_communication": false, "conversation": false },
      "estimates": "comparator baseline only — NOT direct Get Active/imin superiority unless a direct controlled comparison is run (WP §13.1, §19.3)" },
    { "id": "P1", "surface": "public",
      "label": "evidence-aware guided / list interface, identical terminal backend semantics",
      "capabilities": { "evidence_communication": true, "conversation": false },
      "estimates": "evidence-communication / interface effect vs P0" },
    { "id": "P2", "surface": "public",
      "label": "P1 plus conversational clarification / query repair",
      "capabilities": { "evidence_communication": true, "conversation": true },
      "estimates": "combined conversational repair/clarification increment vs P1" }
  ],
  "staff_conditions": [
    { "id": "W0", "surface": "staff",
      "label": "versioned static evidence report / credible generic-dashboard control over identical cases",
      "capabilities": { "workbench": false, "nl_assistant": false } },
    { "id": "W1", "surface": "staff",
      "label": "full staff assurance workbench",
      "capabilities": { "workbench": true, "nl_assistant": true } },
    { "id": "W1-NA", "surface": "staff",
      "label": "W1 without the optional natural-language staff assistant",
      "capabilities": { "workbench": true, "nl_assistant": false } }
  ],

  "families": { "public": ["P0", "P1", "P2"], "staff": ["W0", "W1", "W1-NA"] },

  "tasks": [
    { "task_id": "T-swim-croydon",  "scenario": "supported",     "note": "supported_match" },
    { "task_id": "T-climb-havering","scenario": "no_match",       "note": "bounded_non_match" },
    { "task_id": "T-yoga-croydon",  "scenario": "indeterminate",  "note": "evidence_indeterminate" }
  ],
  "scenario_overrides": {
    "_comment": "MUST stay empty. Any per-condition override of a task's scenario would confound the interface effect with a backend difference (C-BLOCK-06). The validator fails if a family's conditions do not all resolve the same decision per task."
  },

  "identification_honesty": "P2 − P1 is NOT a same-envelope cosmetic interface effect if conversation changes the interpreted query. Any such change is recorded as a different task/decision, never silently (WP §13.1).",
  "tuning_freeze": "Treatment tuning (wording/layout/logic) freezes before locked exposure. Post-freeze accessibility/security repairs are classified; if they change semantics or task flow, the affected conditions are versioned and re-run (WP §13.4).",
  "no_study_fallback": "If ethics/recruitment/readiness fails: retain deterministic conformance, accessibility/AT, semantic-replay / non-interference / security tests; remove usability, practitioner-actionability, organisational-effectiveness and lived-equity claims (WP §13.5).",
  "owner": "Clarence builds the frozen conditions; Fahmi owns estimands, sampling, annotation and analysis (RATIFY-14-07/08)."
}
